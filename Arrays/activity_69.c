#include<stdio.h>

// a program to find sum of lower triangular elements of the matrix

int main()
{
    int n, i, j , sum = 0;

    printf("Enter the number of rows(n) of the matrix: ");
    scanf("%d", &n);

    

}